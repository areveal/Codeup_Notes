#Day 20

##Jason

###Alt Syntax

####short php

* `<?php ?>`- can be  `<? ?>`
	* `php` uneccesary
* `<? echo "..."?>` - can be  `<?= "..."?>` 
	* `=` takes the place of `echo`
* `<? if () {}?>` - can be `<? if () : endif; ?>`
	* getting rid of brackets helps to keep things clean